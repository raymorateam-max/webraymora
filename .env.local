# WebRaymora — local secrets (gitignored, never commit)
# Copy real values into here. Everything else falls back to lib/config.ts.

# --- Lead notifications: email every "Book a call" submission to your Gmail ---
# 1) Enable 2-Step Verification on the Gmail account.
# 2) Create an App Password: Google Account → Security → 2-Step Verification →
#    App passwords → generate for Mail (16 chars, spaces OK).
# 3) Paste it below — that's the inbox that receives the leads.
GMAIL_USER=raymora.team@gmail.com
GMAIL_APP_PASSWORD=cjzncsamvcbmdymk
LEAD_NOTIFY_EMAIL=raymora.team@gmail.com

# --- Supabase (optional for now; service-role key is server-side only) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- n8n webhook → Google Sheets tracker (optional) ---
N8N_WEBHOOK_URL=

# --- Quick-contact links (optional; defaults already point to the real line) ---
NEXT_PUBLIC_WHATSAPP_LINK=https://wa.me/923712385700